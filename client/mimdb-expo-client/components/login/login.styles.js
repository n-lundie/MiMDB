'use strict';

import { StyleSheet } from 'react-native';

export const LoginStyles = StyleSheet.create({
  view: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  input: {
    width: 150,
    height: 30,
    borderWidth: 1,
    borderColor: 'black',
    marginVertical: 1,
  },
});
